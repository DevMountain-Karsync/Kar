angular.module('karSync')
.controller('dashDisplayCtrl', function($scope, userServ,$state,$stateParams){
// i don't think we need this controller --cameron
// console.log($scope.userList);
// console.log($scope.user);

});
